apiVersion: apps/v1
kind: Deployment
metadata:
  name: bluetooth-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bluetooth
  template:
    metadata:
      labels:
        app: bluetooth
    spec:
      containers:
        - name: backend
          image: bluetooth-backend
          ports:
            - containerPort: 5000